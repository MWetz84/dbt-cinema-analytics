name: 'dbt_cinema_analytics'
version: '1.0.0'
config-version: 2

profile: 'default'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_cinema_analytics:  # Match the project name (lowercase)
    staging:
      +materialized: view
    intermediate:
      +materialized: table
    marts:
      +materialized: table